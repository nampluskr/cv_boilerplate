# N6 적대적 검증 기록 및 최종 완주 검증

- **구현자**: Claude Code (Sonnet 5)
- **검토 모델**: Codex (`gpt-5.6-sol`, `codex exec --sandbox read-only`)
- **대상 파일**: 노트북 9개 전체(`notebooks/toy/*.ipynb` 5개, `notebooks/tasks/*.ipynb` 4개) +
  `notebooks/common/*.py`
- **실행 일시**: 2026-08-19
- **실행 횟수**: 1/1 (v0.2 attemptLimitNote에 따라 상한 1회)

## 심각도별 건수

- Critical: 0
- Major: 2
- Minor: 4

## 지적 및 처리

| # | 등급 | 대상 | 지적 | 처리 |
|---|---|---|---|---|
| 1 | Major | `notebooks/tasks/*` 4개 공통 | `check_artifact()`는 파일 존재만 확인하고, checkpoint/leaderboard/control_report.json/config.resolved.yaml이 서로 같은 벤치마크 실행 결과인지는 보장하지 않는다 (예: `--overwrite` 재실행이 중간에 중단되면 일부 파일만 새로 갱신될 수 있다) | 수정: 통제 조건 확인 셀에 `control_report.json`의 `expected_splits`와 `leaderboard.csv`의 `split` 열이 노트북이 기대하는 `MODEL_SPLITS`와 정확히 같은 집합인지 `assert`로 대조하는 코드 추가 (4개 노트북 공통) |
| 2 | Major | `03_detection.ipynb` | N=0 실증이 `custom_fcos_det` 하나로만 이루어져, `fasterrcnn_r50_fpn_det`/`yolov8n_det`의 `train_step`이 N=0을 실제로 받아들이는지는 확인되지 않음 (세 모델의 배치 구성 로직이 서로 완전히 다르다 -- 특히 `yolov8n`은 자체 batch 변환 로직이 있다) | 수정: 3개 모델 섹션에서 checkpoint를 이미 로드한 `fcos_model`/`rcnn_model`/`yolo_model` 전부에 대해 같은 N=0 배치로 `forward()`와 `train_step()`을 직접 호출하는 "3.4 세 모델 모두 N=0을 통과시키는지 확인" 절 추가 |
| 3 | Minor | `00_engine.ipynb` | "`config.resolved.yaml`은 ... 유일한 근거다"라는 표현이 과함 -- 체크포인트에도 같은 config가 저장된다는 것을 노트북 스스로 앞서 언급했음 | 수정: "유일한 근거" -> "사람이 읽기 편한 감사 자료 (체크포인트에도 저장되지만 매번 로드해 열어보는 대신 이 파일 하나로 확인)"로 재작성 |
| 4 | Minor | `01_classification.ipynb` | 요약이 "model.name만 다르다"고 했지만 실제로는 `model.params`(`backbone_name`/`weights_path` 등)도 다르다 -- 초기 draft 문구가 N4 라운드 수정에서 빠짐 | 수정: "model 섹션(이름과 params) 전체가 통제 대상이 아니라 비교 변수 그 자체"로 재작성 |
| 5 | Minor | `02_segmentation.ipynb` | "ASPP head 유무가 유일한 차이"라는 표현이 3.2/3.3 모델 섹션과 요약에 반복됨 -- 실제로는 head 내부 채널 수(256 대 512)와 dropout(0.5 대 0.1)도 다르다(N5에서 비교 섹션 한 곳만 수정하고 나머지를 놓침) | 수정: 3장 도입부, 3.3절, 요약 3곳 모두 "ASPP 유무가 가장 뚜렷하지만 유일한 차이는 아니다"로 통일, classifier 출력 비교를 근거로 명시 |
| 6 | Minor | `04_anomaly.ipynb` | "정상 이미지를 제외하면 pixel AUROC가 **과대평가**된다"는 방향을 단정 -- 실제로는 점수 분포에 따라 오르내릴 수 있어 방향을 보장할 수 없다 | 수정: "방향은 모델의 점수 분포에 따라 다르지만, 어느 쪽이든 실제 배포 모집단과 다른 값을 보고하게 된다는 점에서 잘못된 평가"로 재작성 (§1 데이터 절과 요약 두 곳) |

## 완주 검증 (PLAN §12 성공 기준 대조)

| # | 기준 | 결과 |
|---|---|---|
| 1 | 9개 노트북이 `pytorch_env` 커널에서 처음부터 끝까지 오류 없이 완주 | 통과 -- 9개 전체 `jupyter nbconvert --to notebook --execute --inplace` 재실행, 오류 0건 |
| 2 | `notebooks/toy` 5개가 로컬 데이터셋·체크포인트 없이 완주 | 통과 -- `outputs/benchmarks/`를 임시로 치운 상태에서 5개 전체 재실행, 오류 0건 (toy 데이터는 애초에 `/mnt/d/datasets`·`/mnt/d/backbones`를 참조하지 않는다) |
| 3 | `notebooks/tasks` 4개가 12모델 전부의 예측 시각화와 중간 feature map을 생성 | 통과 -- 4개 노트북 x 3모델 = 12모델 각각에 `FeatureMapRecorder`+`show_feature_maps`, `adapter.predict_step`+`show_predictions` 셀이 있고 전부 오류 없이 실행됨 |
| 4 | 각 태스크 노트북의 비교 섹션 수치가 v0.1 leaderboard와 일치 | 통과(자명) -- 비교 섹션이 `outputs/benchmarks/*_baseline/leaderboard.csv`를 `load_leaderboard()`로 직접 읽어 표시하므로 별도 산출값이 아니라 leaderboard 원본 그 자체다 |
| 5 | EDA가 산출한 split별 샘플 수가 `configs/splits/` 정의와 일치 | 통과 -- 4개 노트북 모두 `DATASETS.build(..., split_path="configs/splits/...")`로 실제 split 파일을 그대로 로드해 `len(dataset)`을 출력하므로 정의와 항상 일치한다 (cls 370/74/74, seg 296/74/74, det 296/74/74, anomaly 209/33/50) |
| 6 | `src/` 아래 파일을 수정하지 않고, 학습 로직을 재구현하지 않음 | 통과 -- `git status --short src/ configs/` 결과 없음(무수정). 모든 학습·추론은 `Trainer.fit`/`adapter.train_step`/`adapter.predict_step`/`model.forward()` 등 `src/`의 실제 함수를 직접 호출 |
| 7 | 체크포인트가 없는 환경에서 `notebooks/tasks`가 재생산 안내와 함께 명확히 실패 | 통과 -- 4개 노트북 모두 벤치마크 디렉터리를 임시로 치운 상태에서 재실행해 준비 확인 셀이 `FileNotFoundError`(재생산 커맨드 포함)로 즉시 멈추는 것을 확인 |
| 8 | 노트북을 대상으로 한 반대 벤더 적대적 검증에 미해결 Critical이 없음 | 통과 -- N3/N4/N5/N6 총 4회 Codex 검토에서 Critical 0건. Major/Minor는 모두 위 표대로 수정 완료 |

## 재검증

N6에서 발견된 수정 사항을 반영한 뒤, 9개 노트북 전체와 `notebooks/toy` 5개의 오프라인 조건을
다시 실행해 모두 통과하는 것을 확인했다 (위 표와 동일). Codex 재검토는 v0.2 정책상 실행하지 않음.

## N3~N5 Major/Minor 처리 총괄

- N3(toy 5개): Major 7건, Minor 3건 -- 전부 서술 정확성 수정 (`docs/dev/v0.2/reviews/N3.md`)
- N4(classification): Major 2건, Minor 1건 -- 통제 조건 확인을 실제 `enforce_control()` 결과 재사용으로 교체, 아티팩트 preflight 확장 (`docs/dev/v0.2/reviews/N4.md`)
- N5(seg/det/anomaly): Major 5건, Minor 4건 -- 보간 계약 실증 추가, ASPP 단정 완화, N=0 실측 오류 정정, anomaly threshold 복원 추가, 공통 학습곡선 누적 버그 수정(`notebooks/common/curves.py`) (`docs/dev/v0.2/reviews/N5.md`)
- N6(전체 통합): Major 2건, Minor 4건 -- 아티팩트 일관성 확인 추가, 3모델 N=0 실증 확장, 잔여 서술 정확성 정정 (본 문서)

모든 라운드에서 Critical은 0건이었고, Major/Minor는 전부 수정을 반영했다. 미해결 위험은 없다.
