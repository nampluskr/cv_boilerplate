# N3 적대적 검증 기록

- **구현자**: Claude Code (Sonnet 5)
- **검토 모델**: Codex (`gpt-5.6-sol`, `codex exec --sandbox read-only`)
- **대상 파일**: `notebooks/toy/00_engine.ipynb`, `notebooks/toy/01_toy_cls.ipynb`,
  `notebooks/toy/02_toy_seg.ipynb`, `notebooks/toy/03_toy_det.ipynb`,
  `notebooks/toy/04_toy_anomaly.ipynb`, `notebooks/common/*.py`
  (00_engine은 N2에서 별도 검증하지 않고 backlog.json N2 방침대로 이 회차에 포함했다)
- **실행 일시**: 2026-08-19
- **실행 횟수**: 1/1 (v0.2 attemptLimitNote에 따라 상한 1회)

## 심각도별 건수

- Critical: 0
- Major: 7
- Minor: 3

## 지적 및 처리

Codex는 Critical 없음, Major 7건, Minor 3건을 보고했다. v0.2 정책상 재검토 없이 1회만 실행하므로,
Major/Minor 전부를 검토해 실제 결함인지 판단하고 전부 수정했다 (허위 주장 없음 확인, 반박 없음).

| # | 등급 | 위치 | 지적 | 처리 |
|---|---|---|---|---|
| 1 | Major | `00_engine.ipynb` 1장 | `toy_cls.yaml`에는 `_base`가 없어(실제로 `configs/toy/_base.yaml`은 `toy_cls.yaml`과 내용이 동일한 파일이다) 상속 예제가 상속을 보여주지 못함 | 수정: `_base: _base.yaml`을 실제로 쓰는 `configs/classification/resnet50.yaml`로 상속 예제를 교체 (YAML 파싱만 하므로 오프라인 안전). 이후 셀은 다시 `toy_cls.yaml`로 override/검증을 진행 |
| 2 | Major | `00_engine.ipynb` 1장 | `predict` 커맨드도 `config.resolved.yaml`을 저장한다고 잘못 서술 (`src/cli/commands.py`의 `predict()`는 저장하지 않음, `train`/`evaluate`/`benchmark`만 저장) | 수정: 문구에서 `predict`를 제외하고 이유(재학습하지 않으므로 공정 비교 대상이 아님)를 추가 |
| 3 | Major | `00_engine.ipynb` 3장 | `type(ADAPTERS.get("toy_cls"))`로 감싸 `ABCMeta`(메타클래스)를 출력, 실제 `ToyClsAdapter` 소스가 아님 | 수정: `type()` 래핑 제거, `ADAPTERS.get(...)`이 이미 클래스 자체를 반환함 |
| 4 | Major | `00_engine.ipynb` 5장 | "정상 이미지만 있는 valid 셋"이라는 서술이 틀림 (`toy_anomaly`의 valid split은 절반이 인위적 결함 이미지) | 수정: valid는 정상/이상 혼합이며 threshold가 그 반응 차이를 이용해 계산됨을 명시. "정상 데이터만 학습"은 train split에만 해당함을 구분 |
| 5 | Major | `00_engine.ipynb` 요약 | best 체크포인트 재로드와 `on_fit_end` 호출 순서가 반대로 서술됨 (`Trainer.fit`은 재로드 -> `on_fit_end` -> 재저장 순인데 요약은 반대로 씀, 5장 본문과도 모순) | 수정: 올바른 순서와 그 이유(반대 순서면 훅의 보정 결과가 저장되지 않거나 미보정 가중치를 훅이 보게 됨)로 재작성 |
| 6 | Major | `03_toy_det.ipynb` 2장/요약 | `det_collate`가 "이미지만 스택"한다고 잘못 서술 (실제로는 이미지·target 둘 다 list로 반환, stacking은 어댑터의 `train_step`/`eval_step`/`predict_step` 내부에서 수행) | 수정: `det_collate`는 이미지도 target도 list로 유지하며, 이미지 stacking 위치가 어댑터 내부임을 명시 |
| 7 | Major | `04_toy_anomaly.ipynb` 2장 | anomaly target이 "텐서로 쌓을 수 없어" 커스텀 collate가 필요하다는 서술이 틀림 (동일 키의 dict는 `default_collate`로도 쌓을 수 있음; 실제 이유는 `eval_step`이 "샘플별 dict 리스트" 구조를 기대하는데 `default_collate`는 그것을 "키별로 쌓인 단일 dict"로 접어버리기 때문) | 수정: `default_collate`가 실제로 만드는 구조와 어댑터가 기대하는 구조의 불일치로 재작성 |
| 8 | Minor | `00_engine.ipynb` 3장/요약 | `Trainer`가 7개 추상 메서드만 호출한다는 서술이 훅 4개(`on_fit_start`/`on_fit_end`/`on_epoch_start`/`on_epoch_end`) 호출을 누락 | 수정: 요약에 "7개 추상 메서드 + 4개 훅"으로 명시 |
| 9 | Minor | `02_toy_seg.ipynb` 3장 | 마스크 shape가 고정인 이유를 `num_classes` 때문이라고 잘못 서술 (실제로는 `image_size`가 shape를 결정하고 `num_classes`는 값의 범위만 결정) | 수정: `image_size` 고정이 이유임을 명시하고 `num_classes`는 범위만 결정한다고 구분 |
| 10 | Minor | `04_toy_anomaly.ipynb` 요약 | "모델이 어댑터를 우회한다"는 표현이 과장됨 (`Trainer`는 항상 `ToyAnomalyAdapter.train_step`을 호출하며, 모델은 어댑터 내부 `run_model_step`의 기본 loss 경로만 우회함) | 수정: 어댑터 경계는 항상 지켜지고, 모델이 우회하는 것은 어댑터 내부의 기본 경로뿐이라고 재작성 |

## 재검증

수정 후 `jupyter nbconvert --to notebook --execute --inplace`로 5개 노트북을 모두 재실행해 오류
없이 완주하는 것을 확인했다 (Codex 재검토는 v0.2 정책상 실행하지 않음). `00_engine.ipynb`는 추가로
`outputs/benchmarks/`를 임시로 치운 상태에서도 완주하는 것을 확인해, `toy/` 5개 노트북이 로컬
데이터셋·체크포인트 없이 완주해야 한다는 전제(PLAN §4.3)를 별도로 검증했다.

## 남은 위험

없음. 지적된 결함은 모두 서술 정확성 문제였고, 수정은 서술을 코드의 실제 동작에 맞춘 것뿐이라
코드/노트북의 동작 자체는 바뀌지 않았다. `notebooks/common/*.py`는 축(axis)별로 결함이 보고되지
않았다.
