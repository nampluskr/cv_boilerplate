# N5 적대적 검증 기록

- **구현자**: Claude Code (Sonnet 5)
- **검토 모델**: Codex (`gpt-5.6-sol`, `codex exec --sandbox read-only`)
- **대상 파일**: `notebooks/tasks/02_segmentation.ipynb`, `notebooks/tasks/03_detection.ipynb`,
  `notebooks/tasks/04_anomaly.ipynb` (검토자에게 `notebooks/common/*.py`와 관련 `src/` 코드도
  참조 허용). 검토자에게 저장된 셀 출력(cell outputs)을 실제 실행 근거로 신뢰해도 된다고 명시했다.
- **실행 일시**: 2026-08-19
- **실행 횟수**: 1/1 (v0.2 attemptLimitNote에 따라 상한 1회)

## 심각도별 건수

- Critical: 0
- Major: 5 (02: 2건, 03: 1건, 04: 2건)
- Minor: 4 (02: 2건, 03: 1건, 04: 2건 -- 하나는 3개 노트북 공통 원인)

## 지적 및 처리

| # | 등급 | 노트북 | 지적 | 처리 |
|---|---|---|---|---|
| 1 | Major | 02 | "마스크도 함께 변형되지만 보간 방식이 다르다" 절이 `build_seg_eval_transform()` 소스와 dtype/값범위 확인만 할 뿐, nearest 보간이 실제로 적용된다는 것을 증명하지 못함 (정수값 유지만으로는 증거가 안 됨) | 수정: `SegmentationTransform.__call__` 소스를 추가로 보여주고, 8x8 합성 마스크를 `v2.functional.resize`(tv_tensors.Mask)와 순수 `F.interpolate(mode="bilinear")`로 각각 축소해 nearest만 원본에 없던 중간값을 만들지 않음을 `assert`로 직접 검증하는 셀 추가 |
| 2 | Major | 02 | `deeplabv3_resnet50`과 `fcn_resnet50`의 mIoU 차이를 "ASPP head 유무가 유일한 차이이므로 head 설계의 영향"이라고 단정 -- 두 모델이 서로 다른 COCO 체크포인트 파일에서 backbone을 초기화하므로 backbone 파라미터 자체까지 완전히 통제됐다고 볼 근거가 없음 | 수정: 두 체크포인트가 서로 다른 파일임을 명시하고, 관측된 차이를 head 설계 효과로만 단정할 수 없다고 재작성 |
| 3 | Minor | 02 | custom_unet의 해상도 정렬을 "버림 오차" 때문이라고 설명 -- 실제로는 `Conv2d(k=3,s=2,p=1)`이 `ceil(H/2)`로 올림 처리함 | 수정: "올림"으로 정정, 원인을 정확히 기술 |
| 4 | Minor(공통) | 02·03·04 | `metrics_epoch.csv`가 v0.1 개발 중 여러 번 재실행되어 append된 상태(실제로 5~6개 run이 누적)인데 `plot_training_curves`가 전체를 하나의 epoch 축으로 이어 그려, epoch 5→1로 되돌아가는 그래프가 됨 | 수정(공통 코드): `notebooks/common/curves.py`에 `_last_run()`을 추가해 마지막 `epoch==1, split=="train"` 지점부터만(= 실제 `best.pth`에 대응하는 run) 플롯하도록 변경. CLAUDE.md 규칙에 따라 공통 코드 변경 후 이미 완료된 N4의 `01_classification.ipynb`도 재실행해 회귀가 없는지 확인함 |
| 5 | Major | 03 | 이미지당 객체 수 실측 결과가 실제로는 N=1(3670장)/N=2(1장)뿐이고 N=0은 이 데이터에 없는데, 요약이 "0과 2 이상도 실제로 존재한다"고 잘못 단정. N=0 배치 통과도 실제로 시연하지 않고 "이 배치엔 없음(다른 배치에서 존재)"이라고만 적음 | 수정: EDA 직후에 이 모집단(XML 주석이 있는 이미지만)은 정의상 N=0을 포함하지 않는다고 정정하고, `toy/03_toy_det.ipynb`와 같은 방식으로 배치의 sample 0을 인위적으로 빈 `(0,4)`/`(0,)` target으로 바꿔치기해 `adapter.train_step`/`eval_step`에 직접 통과시키는 셀로 교체. 요약 문구도 "실제로 존재한다"에서 "인위적으로 구성해 통과를 확인했다"로 정정 |
| 6 | Minor | 03 | (위 #4와 동일한 공통 원인) | 공통 수정 #4에 포함 |
| 7 | Major | 04 | checkpoint만 로드하고 `adapter.image_threshold`/`pixel_threshold`가 `None`인 채로 `predict_step`/시각화를 실행 -- `is_anomalous`가 전부 `None`이 되고 이진화 패널이 `visualize.py`의 fallback(threshold=0.0)으로 그려짐. 재학습(`on_fit_end`)을 하지 않으므로 threshold가 채워질 방법이 없었음 | 수정: `notebooks/common`이 아니라 노트북 자체에 `restore_adapter_thresholds(split_name)` 헬퍼를 추가해, v0.1이 `on_fit_end`에서 계산해 저장한 실제 값(`metrics_final.json`의 `image_threshold`/`pixel_threshold`)을 각 모델 섹션에서 복원한 뒤 `predict_step`/`show_predictions`를 호출하도록 변경. 준비 확인 셀에도 `metrics_final.json` 존재 확인 추가 |
| 8 | Major | 04 | `on_fit_start`/`on_fit_end`가 train-only/valid-only loader만 받는다는 주장을 이 노트북 자체는 독립적으로 검증하지 않음(재학습을 하지 않으므로) | 수정: 문구를 "이 노트북에서 직접 확인했다"가 아니라 "`00_engine.ipynb`가 spy로 이미 검증한 결과를, 재학습 없이 저장된 체크포인트로 다시 확인한다"로 정정해 검증 주체와 범위를 명확히 함 |
| 9 | Minor | 04 | `efficientad`를 "AE·distillation을 결합"이라고 요약해 `custom_ae`처럼 원본 이미지를 재구성한다고 오독될 수 있음. 실제로는 teacher의 정규화된 출력 특징을 재구성한다 | 수정: "특징 공간 재구성(teacher 출력을 이미지로부터 재현)"으로 정정하고 `custom_ae`의 이미지 재구성과 명시적으로 구분 |

## 재검증

수정 후 3개 노트북과, 공통 코드(`notebooks/common/curves.py`) 변경의 영향을 받는 N4의
`01_classification.ipynb`까지 총 4개를 `jupyter nbconvert --to notebook --execute --inplace`로
재실행해 모두 오류 없이 완주하는 것을 확인했다. `restore_adapter_thresholds`가 세 anomaly 모델
각각에 대해 `metrics_final.json`의 실제 값(예: custom_ae image_threshold=7.4908,
pixel_threshold=0.1507)을 정확히 복원하는 것도 출력으로 확인했다. Codex 재검토는 v0.2 정책상
실행하지 않음.

## 남은 위험

없음. 발견된 Major 지적은 모두 "노트북의 주장을 코드가 실제로 증명하지 못하는" 유형이었고, 수정은
전부 (a) 실제로 검증하는 코드를 추가하거나 (b) 검증되지 않은 주장을 검증 가능한 범위로 좁히는
방식으로 처리했다. `_last_run()` 수정은 공통 코드이므로 N6에서 9개 노트북 전체 재실행 시 다시 한 번
회귀 여부를 확인한다.
